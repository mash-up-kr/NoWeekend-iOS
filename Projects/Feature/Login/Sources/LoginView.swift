import SwiftUI

import Domain
import DesignSystem
import Common

public struct LoginView: View {
    public init() {}
    
    public var body: some View {
        VStack {
            Text("시종")
                .font(Typography.title)
        }
    }
}
